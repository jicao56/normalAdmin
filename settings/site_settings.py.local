# -*- coding: utf-8 -*-

from base_settings import *


class Settings(BaseSettings):
    env = ENV_LOCAL

    # 接口端口
    web_port = 9000

    # mysql数据库登录用户名
    mysql_user = 'root'

    # mysql数据库登录密码
    mysql_password = 'root'

    # mysql数据库地址
    mysql_host = '127.0.0.1'

    # 验证码有效期
    redis_captcha_expire_time = 60 * 60 * 24 * 30

    # token有效期
    redis_token_expire_time = 60 * 60 * 24 * 30
